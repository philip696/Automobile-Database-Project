# Automobile-Database-Project

there are 2 versions, the first version, the account data is stored to a database in the form of a table with each car of the user being stored there. The second version, the account data is stored in one table with primary keys and the car of each user is identified by assignning a foreign key.
